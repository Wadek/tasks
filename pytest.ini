[pytest]
testpaths = tests
pythonpath = app
# Prefer verbose locally/CI so humans see each test name (O_VERIFY visibility).
addopts = -v --tb=short
# Avoid pytest-base-url requiring --base-url for our fixtures
base_url = http://127.0.0.1/
